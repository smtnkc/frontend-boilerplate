const fav = require("./../favicon.png");
const css = require('./../styles/main.scss');

import React from 'react';
import ReactDOM from 'react-dom';

ReactDOM.render(
  <h1>Contact page react component!</h1>,
  document.getElementById('reactor')
);